#+title: rugit – Project Tasks

* Tasks

** DONE Scaffold Cargo project (Cargo.toml, directory layout)
** DONE Backend trait and shared data model
** DONE Git backend – read operations (status, log, diff, branches)
** DONE Git backend – write operations (stage, unstage, discard, commit, stash)
** DONE jj backend – subprocess stub
** DONE Status buffer UI (staged / unstaged / untracked sections, spacing, colours)
** DONE Basic keybindings and event loop
** DONE Config loading (~/.config/rugit/config.toml)
** DONE Inline diff expansion in status buffer (Tab toggle)
** DONE Push (=P=) and pull (=F=) via subprocess
** DONE Recent commits section in status buffer
** DONE Commit amend (=c a=) – reopen editor with last commit message pre-filled, replace HEAD
** DONE In-house commit message editor (TUI text area with basic Vim bindings: =i=, =Esc=, =:wq=) instead of deferring to =$EDITOR=
** DONE Push submenu (=P p= push, =P f= force-with-lease) – Magit-style transient
** DONE Vim Normal mode cursor movement in editor (=h j k l= / arrow keys)
** DONE Suppress TUI flash on push/pull (run inline, show Pushing… status)
** DONE Reorder status sections to match Magit (Untracked → Unstaged → Staged)
** DONE C-c C-c to save/quit inline editor (Emacs style)
** TODO Log buffer (=l= – commit graph, navigation)
** TODO Diff viewer buffer (=d= on a commit or file)
** TODO Hunk-level staging
** TODO Line-level staging
** TODO Branch management popup (=b=)
** TODO Stash popup (=z=)
** TODO Rebase menu (=r=)
** TODO Merge menu (=m=)
** TODO Help popup (=?=)
** TODO Interactive rebase UI
** TODO jj backend – full implementation
** TODO Colour themes (catppuccin, gruvbox, nord)
** TODO Packaging (Homebrew formula, AUR PKGBUILD, Nix flake)
